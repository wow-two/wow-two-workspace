# Inline documentation

*Last updated: 2026-08-15*

> Pointer only — the `//` and `/* */` rules live in `code-style/`.

- Imperative one-liner, one line with no exception → [../code-style/documentation.md](../dotnet/lla/documentation.md) § *Inline comments (within method bodies)*
- What never goes in a comment (change narration, rationale essays, `<example>` tags) → [../code-style/documentation.md](../dotnet/lla/documentation.md) § *What NOT to document*
- Which audience a fact belongs to, and therefore whether it is a `//` at all → [../code-style/documentation.md](../dotnet/lla/documentation.md) § *Where a fact belongs*
