# Conventions — Development — Backend

*Last updated: 2026-08-16*

> Stack index. Backend rules live under the stack that owns them, so a second stack lands beside the first
> instead of being folded into it.

| Stack | Index |
|---|---|
| .NET | [dotnet/dotnet-conventions.md](dotnet/dotnet-conventions.md) |

Each stack is cut by **scope** — `lla/` one symbol, `mla/` one service, `hla/` between our own services.
A rule that holds regardless of stack belongs one level up, in [../development-conventions.md](../development-conventions.md).
